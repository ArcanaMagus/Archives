import nodejs import function(hack) import GUIx import mainframe.i create [ function (hack) ; ent.username { if (var i= ; var x = 1000;) i += x; console.log(i); ent.script : username(x >> i [hack] });
